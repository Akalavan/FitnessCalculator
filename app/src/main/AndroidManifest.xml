<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.akalavan.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/pglxuoeviia"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TargetLayout" />
        <activity android:name=".training.TrainingLayout" />
        <activity android:name=".eating.AddEatingLayout" />
        <activity android:name=".training.ContainerTrainingDay" />
        <activity android:name=".report.reportEat" />
        <activity android:name=".report.reportTraining" />
        <activity android:name=".eating.EatingList" />
        <activity android:name=".Help" />
    </application>

</manifest>